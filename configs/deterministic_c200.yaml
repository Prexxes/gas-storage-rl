environment_config:
  environment_name: deterministic
  capacity: 200
  episode_length: 365
  injection_rate: 1.0
  withdrawal_rate: 1.0
  initial_inventory: 0.0
  target_terminal_inventory: 0.0
  initial_inventory_mean_fraction: 0.30
  initial_inventory_std_fraction: 0.05
  price_scale: 2.0
  reward_scale: 2.0
  penalty_factor: 1.0
  efficiency: 1.0
  transaction_cost: 0.0
  leakage: 0.0
dataset_config: {n_pretrain_paths: 5000, n_train_paths: 5000, n_validation_paths: 500, n_test_paths: 1000, cache_dir: data/cache, use_cache: true, force_regenerate: false}
training_config: {total_timesteps: 500000, eval_freq: 20000, n_seeds: 10}
evaluation_config: {deterministic: true, lsmc_action_grid: [-1.0, -0.5, 0.0, 0.5, 1.0], lsmc_n_inventory_levels: 21}
plotting_config: {plot_split: validation, plot_n_paths: 50}
seeds: {master_seed: 201, dataset_seed: 2101, env_seed: 2102, agent_seed: 2103, eval_seed: 2104, plot_seed: 2105}
price_process_config: {seasonal_level: 2.0, seasonal_amplitude: 1.0, seasonal_period: 365.0, ou_speed_of_mean_reversion: 1.0, ou_long_term_mean: 0.0, ou_volatility: 1.2, ou_start_value: 0.0, ou_time_step: 0.0027397260273972603, jump_probability: 0.02, jump_mean: 0.0, jump_std: 0.35, stress_probability: 0.005, stress_multiplier: 1.5}
agent_config: {ppo: {policy: MlpPolicy, device: cpu, gamma: 1.0, n_steps: 2048, batch_size: 256, n_epochs: 10, learning_rate: 0.0003, ent_coef: 0.01}, sac: {policy: MlpPolicy, device: cpu}, td3: {policy: MlpPolicy, device: cpu}}
logging_config: {run_dir: runs}
