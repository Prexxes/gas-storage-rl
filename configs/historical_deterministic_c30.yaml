environment_config:
  environment_name: historical_deterministic
  capacity: 30
  episode_length: 365
  injection_rate: 1.0
  withdrawal_rate: 1.0
  initial_inventory: 0.0
  target_terminal_inventory: 0.0
  price_scale: 50.0
  reward_scale: 50.0
  penalty_factor: 0.5
  efficiency: 1.0
  transaction_cost: 0.0
  leakage: 0.0
dataset_config:
  n_pretrain_paths: 5000
  n_train_paths: 5000
  n_validation_paths: 500
  n_test_paths: 1000
  cache_dir: data/cache
  backtest_cache_dir: data/cache/backtest
  use_cache: true
  force_regenerate: false
historical_data_config:
  monthly_calibration_csv: data/gas_price_data_splits/monthly_calibration_2016_2024.csv
  daily_calibration_csv: data/gas_price_data_splits/daily_calibration_calendar_ffill_2022_2024.csv
  daily_backtest_csv: data/gas_price_data_splits/daily_backtest_tradingdays_2025_2026.csv
  calibration_end_date: "2024-12-31"
  backtest_start_date: "2025-01-01"
calibrated_price_process_config:
  enabled: true
  environment_name: historical_deterministic
  jump_threshold_sigma: 3.0
  backtest_window_stride: 1
training_config: {total_timesteps: 500000, eval_freq: 20000, n_seeds: 10}
evaluation_config: {deterministic: true, lsmc_action_grid: [-1.0, 0.0, 1.0]}
plotting_config: {plot_split: validation, plot_n_paths: 50}
seeds: {master_seed: 1101, dataset_seed: 11101, env_seed: 11102, agent_seed: 11103, eval_seed: 11104, plot_seed: 11105}
price_process_config: {base_price: 50.0, seasonal_amplitude: 0.25, seasonal_phase: 0.0, ou_mean_reversion: 0.08, ou_volatility: 0.12, jump_probability: 0.02, jump_mean: 0.0, jump_std: 0.35, stress_probability: 0.005, stress_multiplier: 1.5}
agent_config: {ppo: {policy: MlpPolicy, device: cpu, gamma: 1.0, n_steps: 2048, batch_size: 256, n_epochs: 10, learning_rate: 0.0003, ent_coef: 0.01}, sac: {policy: MlpPolicy, device: cpu}, td3: {policy: MlpPolicy, device: cpu}}
logging_config: {run_dir: runs}
